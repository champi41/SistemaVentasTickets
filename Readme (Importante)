Para el correcto funcionamiento el pograma localmente se necesita ejecutar el siguiente comando en la terminal: yarn add react-router-dom

Se requiere crear un .env en la carpeta del proyecto (al mismo nivel de .gitignore ) con el siguiente formato ITE_API_URL=(Link_api)
